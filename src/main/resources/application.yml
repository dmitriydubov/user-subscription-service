server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc://postgresql://localhost:5432/user_sub_db

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true